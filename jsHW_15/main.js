// 1.Напишіть, як ви розумієте рекурсію. Навіщо вона використовується на практиці?
// Рукурсія це коли в функції ця ж сама функція викликає сама себе. На практиці це застосоветься коли нам треба щоб результат функції декілька разів використовувався і змінювався.

let number = +prompt("Введіть ваше число");

function factorialNumber(e) {
  if (e < 0) return;

  if (e === 0) return 1;

  return (result = e * factorialNumber(e - 1));
}

alert(factorialNumber(number));
